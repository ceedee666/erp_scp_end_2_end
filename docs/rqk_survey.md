# A SAP CAP Based Survey Application

* Generate app
* Model DB
   * DB model
   * model service
   * CSV data
   * in memory to real DB
* Model UI
   * add oData V2 proxy
   * create admin list report
* Add authentication
